<h1>Post</h1>
<%= link_to "Add?", new_post_path %>
<div>
<% @posts.each do |post| %>
    <tr>
    <td><%= link_to post.title, post_path(post) %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to "Dealataaaaa", post_path(post), method: :delete, data: { confirm: "NOOOOOO!!"}%></td>
  </tr>
</div><br>
<% end %>

  